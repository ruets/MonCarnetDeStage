{% extends 'base.html.twig' %}

{% block body %}
	<button onclick="window.history.back();" class="btn btn-primary top-0 start-0 m-3"><i class="fa fa-arrow-left"></i> Retour</button>
	<h1 style="text-align:center">CompteEtudiant</h1>

	<div class="margin: auto;">
        
		<table class="table" style="display:table;">
			<tbody style="margin:auto; display:table;">
                <tr>
                    <th></th>
						
					</td>
                    <td> <a href="{{ path('app_etudiant_edit', {'id': compte_etudiant.etudiant.id}) }}">éditer</a></td>
                </tr>
				<tr>
					<th>Id</th>
					<td>{{ compte_etudiant.etudiant.id }}</td>
				</tr>
				<tr>
					<th>NumeroINE</th>
					<td>{{ compte_etudiant.etudiant.numeroINE }}</td>
				</tr>
				<tr>
					<th>Nom, Prenom</th>
					<td>{{ compte_etudiant.etudiant.nom }}
						{{ compte_etudiant.etudiant.prenom }}
				</tr>
				<tr>
					<th>Email</th>
					<td>{{ compte_etudiant.etudiant.email }}</td>
				</tr>
			</tbody>
		</table>
		<br>
		<h2 style="text-align:center">Informations complémentaires</h2>


		<table class="table table-striped table-bordered" style="margin:auto; display:table;">
			<tbody style="margin:auto; display:table;">
				<tr>
					<th>Login</th>
					<td>{{ compte_etudiant.login }}</td>
				</tr>
				<tr>
					<th>Roles</th>
					<td>{{ compte_etudiant.roles ? compte_etudiant.roles|json_encode : '' }}</td>
				</tr>
				<tr>
					<th>Password</th>
					<td>{{ compte_etudiant.password }}</td>
				</tr>
				<tr>
					<th>Parcours</th>
					<td>{{ compte_etudiant.parcours }}</td>
				</tr>
				<tr>
					<th>DerniereConnexion</th>
					<td>{{ compte_etudiant.derniereConnexion ? compte_etudiant.derniereConnexion|date('Y-m-d H:i:s') : '' }}</td>
				</tr>
			</tbody>
		</table>
	</div>

    <div class="d-flex justify-content-center">   
	<a href="{{ path('app_compte_etudiant_edit', {'id': compte_etudiant.id}) }}" class="btn btn-primary col-md-1">Editer</a>
    	
    </div>
<div class="d-flex justify-content-center"> 
    {{ include('Admin/compte_etudiant/_delete_form.html.twig') }}
  </div>

{% endblock %}
