<form method="post" action="{{ path('app_compte_etudiant_delete', {'id': compte_etudiant.id}) }}" onsubmit="return confirm('Confirmez-vous la suppression ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compte_etudiant.id) }}">
    <button class="btn btn-danger col-md-1">Supprimer</button>
</form>
