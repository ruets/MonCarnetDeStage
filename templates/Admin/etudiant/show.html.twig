{% extends 'base.html.twig' %}

{% block title %}Etudiant{% endblock %}

{% block body %}
    <button onclick="window.history.back();" class="btn btn-primary top-0 start-0 m-3"><i class="fa fa-arrow-left"></i> Retour</button>
    <h1 style="text-align:center">Etudiant</h1>

    <table class="table" >
        <tbody style="margin:auto; display:table;">
            <tr>
                <th>Id</th>
                <td>{{ etudiant.id }}</td>
            </tr>
            <tr>
                <th>NumeroINE</th>
                <td>{{ etudiant.numeroINE }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ etudiant.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ etudiant.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ etudiant.email }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex justify-content-center"> 

    <a href="{{ path('app_etudiant_edit', {'id': etudiant.id}) }}" class="btn btn-primary col-md-1">Editer</a>

     </div>

     <div class="d-flex justify-content-center"> 
    {{ include('Admin/etudiant/_delete_form.html.twig') }}

    </div>
{% endblock %}
