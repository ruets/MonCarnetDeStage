{% extends 'base.html.twig' %}

{% block title %}Etudiant{% endblock %}

{% block body %}
    <h1>Etudiant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etudiant.id }}</td>
            </tr>
            <tr>
                <th>NumeroINE</th>
                <td>{{ etudiant.numeroINE }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ etudiant.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ etudiant.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ etudiant.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etudiant_index') }}" class="btn btn-secondary col-md-1">Retour Liste</a>

    <a href="{{ path('app_etudiant_edit', {'id': etudiant.id}) }}" class="btn btn-primary col-md-1">Editer</a>

    {{ include('Admin/etudiant/_delete_form.html.twig') }}
{% endblock %}
