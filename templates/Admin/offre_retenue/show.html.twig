{% extends 'base.html.twig' %}

{% block title %}OffreRetenue{% endblock %}

{% block body %}
    <h1>OffreRetenue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre_retenue.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_offre_retenue_index') }}" class="btn btn-secondary col-md-1">Retour Liste</a>

    <a href="{{ path('app_offre_retenue_edit', {'id': offre_retenue.id}) }}" class="btn btn-primary col-md-1">Editer</a>

    {{ include('Admin/offre_retenue/_delete_form.html.twig') }}
{% endblock %}
