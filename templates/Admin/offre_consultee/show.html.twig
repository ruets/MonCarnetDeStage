{% extends 'base.html.twig' %}

{% block title %}OffreConsultee{% endblock %}

{% block body %}
    <h1>OffreConsultee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre_consultee.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_offre_consultee_index') }}" class="btn btn-secondary col-md-1">Retour Liste</a>

    <a href="{{ path('app_offre_consultee_edit', {'id': offre_consultee.id}) }}" class="btn btn-primary col-md-1">Editer</a>

    {{ include('Admin/offre_consultee/_delete_form.html.twig') }}
{% endblock %}
